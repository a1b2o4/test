#ifndef SCHEMEMODE_H
#define SCHEMEMODE_H

// Enum, который раньше был внутри DiagramScene
enum class SchemeMode {
    Edit,
    Run,
    LockSimulation,
    buildPath
};

#endif // SCHEMEMODE_H
